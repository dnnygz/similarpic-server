version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - DEVICE=cpu
      - MODEL_DIR=models
      - DATA_DIR=data
    restart: unless-stopped

